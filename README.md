# Pump.fun Landing Page

## 🚀 Быстрый деплой

### Для новичков - начните здесь:
1. **INSTALL_NODEJS.md** - установка Node.js на сервере
2. **GITHUB_DEPLOY.md** - настройка GitHub и деплой через репозиторий
3. **CLOUDFLARE_SETUP.md** - настройка Cloudflare
4. **SETUP_COMPLETE.md** - полная пошаговая инструкция

### Дополнительные инструкции:
- **QUICK_START.md** - быстрая инструкция для опытных
- **DEPLOY.md** - подробная инструкция по деплою

## Запуск сервера (локально)

### 1. Запустите прокси-сервер (обязательно!)

Прокси-сервер использует ваши прокси для обхода CORS при парсинге HTML с pump.fun:

```bash
node proxy-server.js
```

Прокси-сервер запустится на `http://localhost:3001`

### 2. Запустите основной сервер для работы с путями вида `/live/ID`:

**Рекомендуется использовать встроенный сервер (поддерживает генерацию мета-тегов для Twitter):**
```bash
node server.js
```

Этот сервер:
- Автоматически определяет запросы от ботов (Twitter, Facebook и т.д.)
- Генерирует правильные мета-теги для превью в социальных сетях
- Поддерживает SPA routing

**Альтернативно, если используете `npx serve`:**
```bash
npx serve . -s -p 3000
```

Флаг `-s` включает режим Single Page Application, который перенаправляет все запросы на `index.html`.
⚠️ **Внимание:** `npx serve` не поддерживает генерацию мета-тегов для ботов, превью в Twitter может не работать.

**Если используете Python:**
```bash
python -m http.server 3000
```
(Требует дополнительной настройки для SPA и не поддерживает генерацию мета-тегов)

**Важно:** Оба сервера должны быть запущены одновременно:
- Прокси-сервер на порту 3001 (для парсинга HTML)
- Основной сервер на порту 3000 (для веб-приложения)

## Использование

После настройки сервера, откройте:
- `http://localhost:3000/live/8fM59LxjW1AgejkZqMd3xciwizJXVYmgJyR486wDpump`

Или используйте hash-based routing (работает без настройки сервера):
- `http://localhost:3000/#/live/8fM59LxjW1AgejkZqMd3xciwizJXVYmgJyR486wDpump`

## Структура проекта

- `index.html` - главная страница
- `css/styles.css` - стили
- `js/app.js` - основная логика
- `js/token-loader.js` - загрузка данных токенов с pump.fun
- `assets/` - изображения и медиа файлы
